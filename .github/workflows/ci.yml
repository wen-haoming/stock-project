name: Frontend CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Use Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      - uses: actions/checkout@v3
      - name : cd web
        run: cd web

      - name: Install dependencies
        run: npm install

      - name: Build
        run: npm build
